# API Module

This module contains the HTTP endpoints for the application.

*Current Status:* Under refactoring. Previous multi-schema and test endpoints have been removed to support the migration to a Single Schema + RLS architecture.
